# What's new

Release notes for hermes-operator are maintained in [`CHANGELOG.md`](https://github.com/UndermountainCC/hermes-operator/blob/main/CHANGELOG.md) at the repo root, following [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) and [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

That file is the source of truth. Read it on GitHub for the rendered version, or grep the raw source for a specific field or feature name.

## Status

v1alpha1. The CRD's `apiVersion` will change at each alpha/beta/stable transition. Pin your operator version if you depend on dashboard panel names, metric labels, or condition reason strings; those are not deprecation-cycled in v1alpha1.
